{
  "name": "adlc-emergency-app",
  "version": "1.0.0",
  "description": "Anaconda-Deer Lodge County Emergency Services App",
  "main": "server/index.js",
  "engines": {
    "node": ">=20.0.0",
    "npm": ">=9.0.0"
  },
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && node index.js",
    "client": "cd client && npm start",
    "install-all": "npm install && cd server && npm install && cd ../client && npm install",
    "build": "cd client && npm run build",
    "start": "cd server && node start-production.js",
    "postinstall": "cd server && npm install && cd ../client && npm install",
    "heroku-postbuild": "npm run build",
    "render-build": "echo 'Starting build process...' && npm install --production=false && echo 'Root dependencies installed' && cd server && npm install --production=false && echo 'Server dependencies installed' && cd ../client && npm install --production=false && echo 'Client dependencies installed' && echo 'Building React app...' && CI=false npm run build && echo 'React build completed' && ls -la build/ 2>/dev/null || echo 'Build directory listing:' && cd ../.. && echo 'Verifying build...' && node verify-build.js && echo 'Build verification passed!' && echo 'Build artifacts are ready for deployment'"
  },
  "keywords": ["emergency", "services", "public-safety"],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}

